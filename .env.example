# Optional: where the master CV JSON is stored.
# Defaults to ~/.resume-mcp/master.json when unset.
# Exported PDFs/DOCX are written to an "exports/" folder next to this file.
RESUME_STORE_PATH=~/.resume-mcp/master.json
